<UserControl x:Class="tspAuto.AramaYap"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:tspAuto"
             xmlns:p="clr-namespace:tspAuto.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <materialDesign:PackIcon
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Kind="Magnify"
                Opacity=".56"/>
            <TextBox
                Name="AramaKutusu"
                FontSize="25"
                Grid.Column="1"
                Margin="8 0 0 0"
                materialDesign:HintAssist.Hint="Arama yap"
                materialDesign:TextFieldAssist.DecorationVisibility="Hidden"
                TextChanged="AramaKutusu_TextChanged"/>
        </Grid>
        <DataGrid Name="MuvekkilSonuc" EnableRowVirtualization="False" CanUserReorderColumns="True" CanUserResizeColumns="True"  CanUserResizeRows="True" CanUserSortColumns="True" 
                  AlternatingRowBackground="Gainsboro" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" AutoGenerateColumns="False" IsSynchronizedWithCurrentItem="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Müvekkil Türü" Binding="{Binding MuvekkilTuru}"/>
                <DataGridTextColumn Header="Noter İsmi" Binding="{Binding NoterIsmi}"/>
                <DataGridTextColumn Header="Vekalet Tarihi" Binding="{Binding VekaletTarihi}"/>
                <DataGridTextColumn Header="Vek. Yevmiye No" Binding="{Binding VekYevmiyeNo}"/>
                <DataGridCheckBoxColumn Header="Ahzu Kabza" AutomationProperties.ItemType="Boolean" Binding="{Binding AhzuKabza}"/>
                <DataGridCheckBoxColumn Header="Feragat" Binding="{Binding Feragat}"/>
                <DataGridCheckBoxColumn Header="İbra" Binding="{Binding Ibra}"/>
                <DataGridCheckBoxColumn Header="Sulh" Binding="{Binding Sulh}"/>
                <DataGridTextColumn Header="Banka" Binding="{Binding Banka}"/>
                <DataGridTextColumn Header="Şube" Binding="{Binding Sube}"/>
                <DataGridTextColumn Header="IBAN No" Binding="{Binding IBANno}"/>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
    